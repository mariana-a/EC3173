# EC3173

Al momento de correr el Jupiter Notebook pueden haber errorers el la carapeta donde deben estar los modelos de los diodos, si esto sussede solo 
debe copiar los modelos a la carpeta default que tiene el jupyter Notebook para buscar las librerias de .lib
